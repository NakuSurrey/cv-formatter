import ATSFormatter from "./components/ATSFormatter.jsx";

// root component — wraps ATSFormatter so main.jsx has one clean entry point
function App() {
  return <ATSFormatter />;
}

export default App;
